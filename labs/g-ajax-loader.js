// Generated by CoffeeScript 1.6.2
(function() {
  var root;

  root = typeof exports !== "undefined" && exports !== null ? exports : this;

  root.initialAjaxLoader = function() {
    var ballsColor, ballsDefault, ballsTime;

    ballsDefault = 6;
    /*默认球个数
    */

    ballsTime = 6;
    /*默认转一圈时间
    */

    ballsColor = '#333';
    return $('.gAjaxLoader').each(function(j, k) {
      var ballColor, balls, divs, i, loopTime, _i;

      $(this).empty();
      /*获得参数
      */

      balls = $.trim($(k).attr('data-balls'));
      balls = balls === '' ? ballsDefault : balls;
      loopTime = $.trim($(k).attr('data-time'));
      loopTime = loopTime === '' ? ballsTime : loopTime;
      ballColor = $.trim($(k).attr('data-color'));
      ballColor = ballColor === '' ? ballsColor : ballColor;
      divs = '';
      for (i = _i = 1; 1 <= balls ? _i <= balls : _i >= balls; i = 1 <= balls ? ++_i : --_i) {
        divs += '<div></div>';
      }
      $(k).append(divs);
      return $(k).find('div').each(function(n, m) {
        return $(m).css({
          'background-color': ballColor,
          'animation': 'r360 ' + loopTime + 's ' + (loopTime / balls) * n + 's infinite linear'
        });
      });
    });
  };

  initialAjaxLoader();

}).call(this);
